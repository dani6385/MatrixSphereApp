name: matrix_sphere
publish_to: 'none'

environment:
  sdk: '>=3.11.0 <4.0.0'

workspace:
  #- apps/**
  - packages/**

dev_dependencies:
  melos: ^3.1.0
  flutter_lints: ^6.0.0
dependencies:
  firebase_core: ^4.12.1
  firebase_crashlytics: ^5.2.6
  flutter:
    sdk: flutter
  shared_services: ^0.0.0
