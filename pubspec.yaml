name: matrix_sphere_workspace
description: Matrix Sphere sistem pengendalian dan moderasi akun otomatis yang berfungsi memelihara kepatuhan aturan dalam platform belanja.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.5.0 <4.0.0'

workspace:
  #- apps/admin_mikrotik
  #- apps/client_connectivity
  #- apps/shop_sphere
  #- apps/seller_sphere
  - packages/shared_services
  - packages/shared_ui
dependencies:
  animations: ^2.0.11
  cloud_firestore: ^6.7.0
  firebase_analytics: ^12.4.5
  firebase_core: ^4.12.1
  firebase_crashlytics: ^5.2.6
  flutter:
    sdk: flutter
  flutter_test:
    sdk: flutter
  intl: ^0.19.0
  provider: ^6.1.5+1
  shared_services: ^1.0.0
  shared_ui: ^0.0.1
