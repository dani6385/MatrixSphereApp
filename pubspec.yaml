name: my_iot_project
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  # CORRECTED: Using the version from pub.dev to match the sub-project
  routeros_api: ^0.1.4
  firebase_core: ^3.15.2
  firebase_auth: ^5.7.0
  cloud_firestore: ^5.6.12
  provider: ^6.1.2
  http: ^1.2.0
  html: ^0.15.4
  crypto: ^3.0.7
  fl_chart: ^0.68.0
  device_info_plus: ^10.0.0
  logger: ^2.0.0
  change_app_package_name: ^1.5.0
  firebase_crashlytics: ^4.3.10
  network_info_plus: ^7.0.0
  connectivity_plus: ^7.1.1
  convert: ^3.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  melos: ^3.1.0
