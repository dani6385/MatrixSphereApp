name: shared_services
description: A starting point for Dart libraries or applications.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: '>=2.17.0 <3.0.0'

dependencies:
  routeros_api: ^0.1.4
  firebase_core: ^2.24.2 # Ditambahkan
  cloud_firestore: ^4.14.0 # Ditambahkan

dev_dependencies:
  lints: ^2.0.0
  test: ^1.16.0
