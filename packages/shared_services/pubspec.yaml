name: shared_services
description: A starting point for Dart libraries or applications.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  routeros_api: ^0.1.4
  # Samakan versinya ke 3.0.0
  firebase_core: ^3.0.0 
  cloud_firestore: ^5.0.0

dev_dependencies:
  lints: ^2.0.0
  test: ^1.16.0
