/// Defines the type of a specific page or feature within an app.
///
/// This is used by factories (like `DrawerFactory`) to determine
/// which specific UI components (e.g., drawer items) to build for a given page.
enum PageType {
  home,
  attendance,
  approvals,
  tasks,
  analytics,
  feeds,
  searching,
  transactions,
  account,
  financial,
  management,
  seller,
}

