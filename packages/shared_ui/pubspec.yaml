name: shared_ui
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  shared_assets:
    path: ../shared_assets
  google_fonts: ^6.2.1 # Menambahkan dependensi yang hilang

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
