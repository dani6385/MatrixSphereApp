import 'package:flutter/material.dart';

class ConnectionTab extends StatelessWidget {
  const ConnectionTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Connection Tab'),
    );
  }
}
