import 'package:flutter/material.dart';

class KoneksiTab extends StatelessWidget {
  const KoneksiTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Koneksi Tab'),
    );
  }
}
