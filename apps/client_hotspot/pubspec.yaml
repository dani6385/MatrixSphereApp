name: ms_connectivity
description: "M|S Connectivity"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  shared_ui:
    path: ../../packages/shared_ui

  cupertino_icons: ^1.0.8
  fl_chart: ^1.2.0
  routeros_api: ^0.1.4
  logger: ^2.7.0
  percent_indicator: ^4.2.5
  permission_handler: ^11.3.1
  provider: ^6.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
