name: client_connectivity
description: "Matrix Sphere Client."
publish_to: none
resolution: workspace
version: 1.0.0+1
environment:
  sdk: ">=3.12.1 <4.0.0"
dependencies:
  flutter:
    sdk: flutter
  shared_services:
    path: ../../packages/shared_services
  shared_ui:
    path: ../../packages/shared_ui
  http: ^1.2.1
  cupertino_icons: ^1.0.8
  google_fonts: ^8.1.0
  firebase_core: ^4.11.0
  firebase_database: ^12.4.3
  get_it: ^7.7.0
  cloud_firestore: ^6.6.0
  fl_chart: ^1.2.0
  permission_handler: ^12.0.3
  firebase_auth: ^6.5.4
  go_router: ^17.3.0
  image_picker: ^1.2.2
  app_settings: ^7.0.0
  internet_connection_checker_plus: ^3.1.0
  internet_speed_test: ^1.5.0
  provider: ^6.1.5+1
  device_info_plus: ^10.1.0
  package_info_plus: ^8.1.0
  network_info_plus: ^6.0.0

flutter:
  uses-material-design: true

dev_dependencies:
  flutter_test:
    sdk: flutter
  melos: ^7.8.2
  flutter_lints: ^6.0.0
workspace:
  # list relative package paths if this package is part of a workspace
  # keep empty list if not using a workspace
  []
melos: {}