package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Sleek Interface Theme Palette
val SleekIndigo = Color(0xFF4F46E5)      // Primary Indigo Accent
val SleekBlue = Color(0xFF3B82F6)        // Secondary Blue Accent
val SleekBackground = Color(0xFFF3F4F9)  // Background body color
val SleekSurface = Color(0xFFFFFFFF)     // Card / Component background
val SleekTextPrimary = Color(0xFF1E293B) // Primary slate text (Slate-800)
val SleekTextSecondary = Color(0xFF64748B) // Secondary slate text (Slate-500)
val SleekTextTertiary = Color(0xFF94A3B8)  // Tertiary slate text (Slate-400)

val RedWarning = Color(0xFFEF4444)
val AmberOrange = Color(0xFFF59E0B)
val GreenSuccess = Color(0xFF10B981)

val BorderLight = Color(0xFFE2E8F0)
val CardSlate = Color(0xFFFFFFFF) // Keep variable name compatibility if referenced
val SoftCyan = Color(0xFFF3F4F9)  // Keep variable name compatibility if referenced
val DeepObsidian = Color(0xFFF3F4F9) // Keep variable name compatibility if referenced
val TextWhite = Color(0xFF1E293B)    // Re-mapped to Slate text to automatically flip most white labels
val TextGray = Color(0xFF64748B)     // Re-mapped to Slate-500
val BorderCyan = Color(0xFFE2E8F0)   // Re-mapped to light border
val CyberCyan = Color(0xFF4F46E5)    // Re-mapped to primary indigo
val ElectricBlue = Color(0xFF3B82F6) // Re-mapped to secondary blue
val GreenSuccessDark = Color(0xFF15803D) // Green-700 for text
val AmberWarningDark = Color(0xFF92400E) // Amber-800 for warning text
val AmberWarningBg = Color(0xFFFFFBEB)   // Amber-50 for warning background
val AmberWarningBorder = Color(0xFFFEF3C7) // Amber-100 for warning border
