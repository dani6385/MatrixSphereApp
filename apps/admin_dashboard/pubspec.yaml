name: admin_dashboard
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.0.0
  firebase_auth: ^5.0.0  # Ditambahkan untuk otentikasi
  cloud_firestore: ^5.0.0
  fl_chart: ^0.68.0
  shared_services:
    path: ../../packages/shared_services
  network_info_plus: ^7.0.0
  device_info_plus: ^12.4.0
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
