name: admin_app
description: A shared provider for real-time Firebase data.
version: 0.0.1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Pastikan path ini mengarah ke folder yang benar
shared_services:
  path: ../../packages/shared_services