
import 'package:flutter/material.dart';

class LabelPrinterScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Label Printer Screen'),
    );
  }
}
