
import 'package:flutter/material.dart';

class ManagementBody extends StatelessWidget {
  const ManagementBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Management Body Content'),
    );
  }
}

