
import 'package:flutter/material.dart';

class AppViewModel extends ChangeNotifier {
  // You can add state and logic related to the overall app view here.
  // For example, managing app-wide settings, themes, or other global UI states.
}