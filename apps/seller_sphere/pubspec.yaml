name: seller_sphere
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.6.0 <4.0.0'

resolution: workspace

dependencies:
  firebase_core: ^4.12.1
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
