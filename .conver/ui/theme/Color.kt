package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Sophisticated Dark Brand Colors
val SophisticatedPrimary = Color(0xFF6366F1)    // Premium Indigo (indigo-500)
val SophisticatedSecondary = Color(0xFF3B82F6)  // Tech Blue (blue-500)
val SophisticatedTertiary = Color(0xFF14B8A6)   // Secure Teal (teal-500)

// Sophisticated Dark Theme Palette
val SophisticatedBackground = Color(0xFF0B0E14)  // Deep slate black
val SophisticatedSurface = Color(0xFF111622)     // Slate-900 semi-transparent card equivalent
val SophisticatedBorder = Color(0xFF1E293B)      // Slate-800 divider / border
val SophisticatedTextPrimary = Color(0xFFFFFFFF)  // Crisp white
val SophisticatedTextSecondary = Color(0xFF94A3B8)// Muted slate (slate-400)

// Maintain old color names to prevent unresolved references in legacy layouts
val CyanPrimary = SophisticatedPrimary
val BlueSecondary = SophisticatedSecondary
val TealTertiary = SophisticatedTertiary
val SlateBackgroundDark = SophisticatedBackground
val SlateSurfaceDark = SophisticatedSurface
val SlateBorderDark = SophisticatedBorder
val TextOnDarkPrimary = SophisticatedTextPrimary
val TextOnDarkSecondary = SophisticatedTextSecondary

// Light Theme Palette (Bright Slate)
val SlateBackgroundLight = Color(0xFFF8FAFC) // Ice white
val SlateSurfaceLight = Color(0xFFFFFFFF)    // Pure white cards
val SlateBorderLight = Color(0xFFE2E8F0)     // Muted borders
val TextOnLightPrimary = Color(0xFF0F172A)   // Dark slate text
val TextOnLightSecondary = Color(0xFF475569) // Charcoal slate

